</br>

<h1>Choose your University</h1>

</br>

<% @universities.each do |university| %>
    <div class="row row-clickable row-uni" data-href="<%= university_path(university) %>">
        <%= university.name %>
    </div>
<% end %>