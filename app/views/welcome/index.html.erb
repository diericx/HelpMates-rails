</br>

<h1>Deez nuts</h1>

</br>

<table class="table table-hover">
    <thead>
        <tr>
            <th scope="col"></th>
        </tr>
    </thead>

    <tbody>
        <% @universities.each do |university| %>
            <tr class="row-uni" data-link="<%= university_path(university) %>">
                <td><%= university.name %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<script>
    $("tr[data-link]").click(function() {
        window.location = $(this).data("link")
    })
</script>
