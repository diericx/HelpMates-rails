</br>

<h1>Find someone to help you with <%= @course.title1 %></h1>

</br>

<div>
  Available Tutors:
</div>

<div class="col">
  
  <% @tutors.each do |id, tutor| %>
    <div class="row row-tutor">
      <div class="col-xs-2">
        <% if tutor.avatar.url.present? %>
          <%= image_tag(tutor.avatar.url, class: "profile-img-sm") %>
        <% end %>
      </div>
      <div class="col-xs-2 my-auto info">
          <%= tutor.name %> </br>
          1st Year </br>
          CS Major </br>
      </div>
      <!-- <table style="height: 100px;">
        <tbody>
          <tr>
            <td class="align-middle">
              
            </td>
            <td class="info">

            </td>
          </tr>
        </tbody>
      </table> -->
    </div>
  <% end %>

</div>

<% link_to 'Edit', edit_course_path(@course) %>
<% link_to 'Back', courses_path %>
